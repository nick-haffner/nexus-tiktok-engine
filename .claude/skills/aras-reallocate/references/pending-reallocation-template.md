# Pending Reallocation

<!--
INSTRUCTIONS:
- All subsystem sections are pre-populated from their ARA.md files.
- All sections are editable. The reallocation target is marked;
  other subsystems may also need scope adjustments to accommodate
  the new task (e.g., releasing paths to the reallocated subsystem,
  adjusting shared scope participants).
- For Exclusive Scope: remove paths no longer needed, add new paths,
  keep paths still relevant. Exclusive scope must not overlap
  between subsystems.
- For Shared Scope: edit if participants change. "None" if none.
  If ANY subsystem declares shared scope, every subsystem named as
  a participant MUST declare matching shared scope.

VALIDATION RULES:
- Reallocation target's new task overview must be non-empty.
- Every subsystem must declare at least one exclusive scope path.
- Exclusive scope must not overlap between subsystems.
- If shared scope is declared, all named participants must declare
  matching shared scope.
-->

## Subsystem: [instance-1] (Reallocation Target)

- **Instance identifier:**
- **Branch:**
- **Previous task:**
- **New task overview (required):**
- **Exclusive scope (required):**
- **Shared scope (required — "None" if none):**
- **Additional restricted paths (optional):**

## Subsystem: [instance-2]

- **Instance identifier:**
- **Branch:**
- **Task overview:**
- **Exclusive scope (required):**
- **Shared scope (required — "None" if none):**
- **Additional restricted paths (optional):**
