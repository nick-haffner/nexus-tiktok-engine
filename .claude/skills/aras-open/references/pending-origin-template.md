# Pending Origination

<!--
VALIDATION RULES:
- Fields marked (required) must be non-empty.
- Fields marked (optional) may be left blank.
- Shared scope: if ANY subsystem declares shared scope, every
  subsystem named as a participant MUST declare matching shared
  scope. Omitting shared scope on a named participant fails validation.
- Exclusive scope must not overlap between subsystems.
-->

## Subsystem 1 (Existing Branch)

- **Source branch (required):**
- **Branch name (required):**
- **Instance identifier (required):**
- **Task overview (required):**
- **Exclusive scope (required):**
- **Shared scope (required — "None" if none):**
- **Additional restricted paths (optional):**
- **Notes (optional):**

## Subsystem 2 (New Branch)

- **Source branch (required):**
- **Branch name (required):**
- **Instance identifier (required):**
- **Task overview (required):**
- **Exclusive scope (required):**
- **Shared scope (required — "None" if none):**
- **Additional restricted paths (optional):**
- **Notes (optional):**
